{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm run test"
  },
  "author": "anju gaurav",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.7.2"
  }
}
